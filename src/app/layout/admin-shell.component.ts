import { Component, inject, signal } from '@angular/core';
import { Router, RouterLink, RouterLinkActive, RouterOutlet } from '@angular/router';
import { MatIconModule } from '@angular/material/icon';
import { TranslatePipe } from '@ngx-translate/core';
import { AdminAuthService } from '../core/auth/admin-auth.service';
import { LanguageService } from '../core/i18n/language.service';
import { ThemeService } from '../core/theme/theme.service';

interface NavItem { label: string; route: string; icon: string; }
@Component({
  selector: 'app-admin-shell', imports: [RouterLink, RouterLinkActive, RouterOutlet, MatIconModule, TranslatePipe],
  template: `
    <div class="shell" [class.collapsed]="collapsed()" (click)="menuOpen.set(false)">
      <aside class="sidebar">
        <a class="brand" routerLink="/menus" aria-label="Meno Admin home"><span class="brand-mark"><mat-icon>hub</mat-icon></span><span class="brand-text"><strong>Panda House</strong><small>MENO ADMIN</small></span></a>
        <button class="collapse" type="button" (click)="toggleSidebar(); $event.stopPropagation()" [attr.aria-label]="collapsed() ? 'Expand navigation' : 'Collapse navigation'"><mat-icon>{{collapsed() ? 'menu' : 'menu_open'}}</mat-icon></button>
        <nav>
          <section><p>{{ 'NAV.KITCHEN_MENU' | translate }}</p>@for (item of kitchen; track item.route) {<a [routerLink]="item.route" routerLinkActive="active" [attr.aria-label]="item.label | translate"><mat-icon>{{item.icon}}</mat-icon><span>{{item.label | translate}}</span></a>}</section>
          <section><p>{{ 'NAV.ANALYTICS' | translate }}</p>@for (item of analytics; track item.route) {<a [routerLink]="item.route" routerLinkActive="active" [attr.aria-label]="item.label | translate"><mat-icon>{{item.icon}}</mat-icon><span>{{item.label | translate}}</span></a>}</section>
          <section><p>{{ 'NAV.AUDIT' | translate }}</p><a routerLink="/audit" routerLinkActive="active" [attr.aria-label]="'NAV.AUDIT' | translate"><mat-icon>assignment</mat-icon><span>{{ 'NAV.AUDIT' | translate }}</span></a></section>
          <section><p>POS SETTINGS</p><a routerLink="/settings" routerLinkActive="active" [attr.aria-label]="'NAV.POS_SETTINGS' | translate"><mat-icon>tune</mat-icon><span>{{ 'NAV.POS_SETTINGS' | translate }}</span></a></section>
          <section><p>{{ 'NAV.MANAGEMENT' | translate }}</p>@for (item of management; track item.route) {<a [routerLink]="item.route" routerLinkActive="active" [attr.aria-label]="item.label | translate"><mat-icon>{{item.icon}}</mat-icon><span>{{item.label | translate}}</span></a>}</section>
        </nav>
        <footer><small>v1.4.2</small><span><b></b><span class="online-text">{{ 'COMMON.ONLINE' | translate }}</span></span></footer>
      </aside>
      <section class="main"><header class="topbar"><div class="crumb"><span>{{ 'MENU.BREADCRUMB' | translate }}</span><mat-icon>chevron_right</mat-icon><strong>{{ 'NAV.MENUS' | translate }}</strong></div><div class="user" (click)="$event.stopPropagation()"><button class="language" type="button" (click)="language.toggle()">{{ language.current() === 'ka' ? 'EN' : 'ქა' }}</button><button class="user-trigger" type="button" (click)="menuOpen.set(!menuOpen())"><span class="avatar">{{ initials() }}</span><span class="user-name"><strong>{{ auth.session()?.user?.firstName }} {{ auth.session()?.user?.lastName }}</strong><small>{{ 'COMMON.OWNER' | translate }}</small></span><mat-icon>keyboard_arrow_down</mat-icon></button>@if (menuOpen()) {<div class="user-menu"><button type="button" (click)="language.toggle()"><mat-icon>language</mat-icon>{{ language.current() === 'ka' ? 'English' : 'ქართული' }}</button><button type="button" (click)="theme.toggle()"><mat-icon>{{ theme.current()==='light' ? 'dark_mode' : 'light_mode' }}</mat-icon>{{ theme.current()==='light' ? ('COMMON.DARK_THEME' | translate) : ('COMMON.LIGHT_THEME' | translate) }}</button><button class="logout" type="button" (click)="logout()"><mat-icon>logout</mat-icon>{{ 'COMMON.SIGN_OUT' | translate }}</button></div>}</div></header><main><router-outlet /></main></section>
    </div>`,
  styles: `
    .shell{display:flex;min-height:100dvh;background:var(--surface-page)}.sidebar{display:flex;position:sticky;top:0;flex:0 0 252px;flex-direction:column;width:252px;height:100dvh;overflow:hidden;background:var(--color-shell);color:var(--color-shell-text);transition:width .22s ease,flex-basis .22s ease}.brand{display:flex;align-items:center;gap:10px;min-height:68px;padding:0 17px;border-bottom:1px solid #ffffff0d;color:inherit;text-decoration:none;white-space:nowrap}.brand-mark{display:grid;width:34px;height:34px;place-items:center;border-radius:50%;background:var(--color-secondary);color:#fff}.brand-mark mat-icon{font-size:22px}.brand strong,.brand small{display:block}.brand strong{font-size:14px;letter-spacing:-.02em}.brand small{margin-top:4px;color:#b2bcb3;font-size:10px;letter-spacing:.16em}.collapse{position:absolute;top:15px;right:12px;display:grid;width:31px;height:31px;place-items:center;border:1px solid #ffffff22;border-radius:10px;background:#ffffff0b;color:#c8d1c9}.collapse:hover{background:#ffffff18}nav{padding:1px 10px;overflow:hidden}nav section{margin:9px 0 12px}nav p{margin:0 8px 5px;color:#849087;font-size:11px;font-weight:800;letter-spacing:.12em;white-space:nowrap}nav a{display:flex;position:relative;align-items:center;gap:10px;min-height:34px;padding:0 10px;border-radius:0 22px 22px 0;color:#b6c0b8;font-size:13px;font-weight:700;text-decoration:none;white-space:nowrap}nav a mat-icon{flex:none;width:20px;height:20px;font-size:20px}nav a:hover{background:#ffffff0a;color:#fff}nav a.active{background:color-mix(in srgb,var(--color-primary) 25%,#17251f);color:#fff}nav a.active:before{position:absolute;left:0;width:3px;height:25px;border-radius:0 3px 3px 0;background:var(--color-secondary);content:''}.sidebar footer{display:flex;justify-content:space-between;margin-top:auto;padding:17px 20px;border-top:1px solid #ffffff12;color:#849087;font-size:12px}.sidebar footer>span{display:flex;align-items:center;color:#9fa99d}.sidebar footer b{width:8px;height:8px;margin-right:7px;border-radius:50%;background:#14dfaa;box-shadow:0 0 9px #14dfaa}.main{min-width:0;flex:1;transition:width .22s ease}.topbar{display:flex;position:sticky;top:0;z-index:10;justify-content:space-between;align-items:center;height:56px;padding:0 24px;border-bottom:1px solid var(--border);background:var(--surface-card)}.crumb{display:flex;align-items:center;gap:8px;font-size:13px}.crumb span{color:var(--text-muted)}.crumb mat-icon{color:var(--border);font-size:19px}.crumb strong{font-size:13px}.user{position:relative;display:flex;align-items:center;gap:11px}.language{height:31px;padding:0 9px;border:1px solid var(--border);border-radius:8px;background:var(--surface-muted);color:var(--text-primary);font-size:12px;font-weight:800}.user-trigger{display:flex;align-items:center;gap:10px;border:0;background:transparent;color:var(--text-primary);text-align:left}.avatar{display:grid;width:36px;height:36px;place-items:center;border-radius:50%;background:var(--color-secondary);color:#fff;font-size:12px;font-weight:800}.user-trigger span>strong,.user-trigger span>small{display:block}.user-trigger span>strong{font-size:14px}.user-trigger span>small{margin-top:2px;color:var(--text-muted);font-size:11px}.user-trigger>mat-icon{margin-left:8px;color:var(--text-muted);font-size:18px}.user-menu{position:absolute;top:52px;right:0;width:210px;padding:7px;border:1px solid var(--border);border-radius:12px;background:var(--surface-card);box-shadow:var(--shadow)}.user-menu button{display:flex;align-items:center;gap:9px;width:100%;padding:10px;border:0;border-radius:8px;background:transparent;color:var(--text-primary);font-weight:700;text-align:left}.user-menu button:hover{background:var(--surface-muted)}.user-menu mat-icon{font-size:19px}.user-menu .logout{color:var(--danger)}.main>main{width:calc(100% / var(--content-scale));min-height:calc(143dvh - 80px);transform:scale(var(--content-scale));transform-origin:top left}.shell.collapsed .sidebar{flex-basis:64px;width:64px}.shell.collapsed .brand{justify-content:center;padding:0}.shell.collapsed .brand-text,.shell.collapsed nav p,.shell.collapsed nav a span,.shell.collapsed footer small,.shell.collapsed .online-text{display:none}.shell.collapsed .collapse{right:20px;top:55px;width:31px;height:31px}.shell.collapsed nav{padding:34px 6px 2px}.shell.collapsed nav section{margin:9px 0}.shell.collapsed nav a{justify-content:center;padding:0;border-radius:12px}.shell.collapsed .sidebar footer{justify-content:center;padding:16px 0}.shell.collapsed .sidebar footer b{margin:0}@media(max-width:850px){.sidebar{flex-basis:64px;width:64px}.brand{justify-content:center;padding:0}.brand-text,nav p,nav a span,.sidebar footer small,.online-text,.collapse{display:none}.sidebar nav{padding:4px 8px}.sidebar nav section{margin:9px 0}.sidebar nav a{justify-content:center;padding:0;border-radius:12px}.sidebar footer{justify-content:center;padding:16px 0}.sidebar footer b{margin:0}.topbar{padding:0 18px}.crumb span,.crumb mat-icon{display:none}.user-name,.user-trigger>mat-icon{display:none}}
  `
})
export class AdminShellComponent {
  readonly auth = inject(AdminAuthService); readonly theme = inject(ThemeService); readonly language = inject(LanguageService); private readonly router = inject(Router);
  readonly menuOpen = signal(false); readonly collapsed = signal(localStorage.getItem('meno-admin-sidebar') === 'collapsed');
  readonly kitchen: NavItem[] = [{ label: 'NAV.MENUS', route: '/menus', icon: 'restaurant_menu' }, { label: 'NAV.CATEGORIES', route: '/categories', icon: 'grid_view' }, { label: 'NAV.DISHES', route: '/dishes', icon: 'soup_kitchen' }];
  readonly analytics: NavItem[] = [{ label: 'NAV.ANALYTICS', route: '/analytics', icon: 'monitoring' }];
  readonly management: NavItem[] = [{ label: 'NAV.STAFF', route: '/staff', icon: 'group' }, { label: 'NAV.ROLES', route: '/roles', icon: 'admin_panel_settings' }];
  toggleSidebar() { this.collapsed.update(value => !value); localStorage.setItem('meno-admin-sidebar', this.collapsed() ? 'collapsed' : 'expanded'); }
  initials() { const user = this.auth.session()?.user; return user ? `${user.firstName[0] ?? ''}${user.lastName[0] ?? ''}` : '?'; }
  logout() { this.auth.signOut(); void this.router.navigateByUrl('/sign-in'); }
}







