import { Component, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { Router } from '@angular/router';
import { AdminAuthService } from '../../core/auth/admin-auth.service';

@Component({ selector:'app-admin-sign-in', imports:[FormsModule], template:`<main class="login"><section class="brand"><div class="mark">P</div><h1>MENO ADMIN</h1><p>Restaurant operations platform</p></section><form (ngSubmit)="submit()"><h2>Welcome back</h2><label>Email<input type="email" name="email" [(ngModel)]="email" required autocomplete="username" /></label><label>Password<input type="password" name="password" [(ngModel)]="password" required autocomplete="current-password" /></label>@if(error()){<p class="error">Enter your admin email and password.</p>}<button type="submit">Sign in</button></form></main>`, styles:`.login{display:grid;min-height:100dvh;place-items:center;padding:24px;background:linear-gradient(135deg,#111a17,#2f6b3f)}.brand{position:absolute;top:12%;color:#fff;text-align:center}.mark{display:grid;width:64px;height:64px;place-items:center;margin:auto;border-radius:50%;background:#f8f4e8;color:#2f6b3f;font-size:34px;font-weight:800}.brand h1{margin:12px 0 2px;letter-spacing:3px}.brand p{margin:0;opacity:.75}.login form{width:min(420px,100%);padding:28px;border-radius:18px;background:var(--surface-card);box-shadow:0 18px 50px #0005}.login h2{margin:0 0 20px}label{display:grid;gap:7px;margin:14px 0;color:var(--text-muted);font-size:13px;font-weight:700}input{min-height:46px;border:1px solid var(--border);border-radius:9px;padding:0 12px;background:var(--surface-muted);color:var(--text-primary)}button{width:100%;min-height:46px;border:0;border-radius:9px;background:var(--olive);color:#fff;font-weight:800;cursor:pointer}.error{color:var(--danger);font-size:12px}`})
export class AdminSignInComponent { private readonly auth=inject(AdminAuthService);private readonly router=inject(Router);email='';password='';readonly error=signal(false);async submit(){this.error.set(false);if(await this.auth.signIn(this.email,this.password))void this.router.navigateByUrl('/settings');else this.error.set(true)}}
