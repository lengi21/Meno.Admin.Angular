import { Component, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { AdminApiService, CatalogTranslations, Category, DishPage } from '../../core/api/admin-api.service';
import { categoryName, emptyTranslations } from '../catalog/catalog-form.utils';
@Component({selector:'app-dishes',imports:[FormsModule],template:`<main class="page"><header><div><p>MENU CATALOG</p><h1>Dishes</h1></div><button (click)="save()">Add dish</button></header><section class="form"><select [(ngModel)]="categoryId">@for(category of categories();track category.id){<option [value]="category.id">{{name(category)}}</option>}</select><input type="number" placeholder="Default price" [(ngModel)]="price"><input type="number" placeholder="Calories" [(ngModel)]="calories"><input placeholder="ქართული სახელი" [(ngModel)]="translations.ka.name"><input placeholder="English name" [(ngModel)]="translations.en.name"><input placeholder="Русское название" [(ngModel)]="translations.ru.name"><input placeholder="Image URL" [(ngModel)]="imageUrl"><label>Upload image <input type="file" accept="image/png,image/jpeg,image/webp" (change)="upload($event)"></label></section><div class="search"><input placeholder="Search dishes" [(ngModel)]="search" (ngModelChange)="load(1)"><span>{{page().total}} dishes</span></div><section class="list">@for(dish of page().items;track dish.id){<article><img [src]="dish.imageUrl || ''" alt=""><div><strong>{{dishName(dish)}}</strong><span>{{dish.priceAmount}} · {{dish.status}}</span></div></article>}@empty{<p>No dishes found.</p>}</section><footer><button (click)="load(page().page-1)" [disabled]="page().page===1">Previous</button><span>Page {{page().page}}</span><button (click)="load(page().page+1)" [disabled]="page().page*page().pageSize>=page().total">Next</button></footer></main>`,styles:`.page{padding:28px;max-width:1100px}header{display:flex;align-items:center;justify-content:space-between}header p{margin:0;color:var(--olive);font-weight:800;font-size:12px}h1{margin:4px 0 20px}button{border:0;border-radius:8px;padding:10px 14px;background:var(--olive);color:#fff;font-weight:800}.form{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;padding:16px;background:var(--surface-card);border:1px solid var(--border);border-radius:13px}input,select,label{box-sizing:border-box;width:100%;min-height:42px;padding:0 10px;border:1px solid var(--border);border-radius:8px;background:var(--surface-muted);color:var(--text-primary)}label{display:flex;align-items:center;gap:8px}label input{border:0;padding:0}.search{display:flex;gap:10px;align-items:center;margin:16px 0}.search input{max-width:360px}.search span{color:var(--text-muted)}.list{display:grid;grid-template-columns:repeat(auto-fill,minmax(210px,1fr));gap:10px}article{display:flex;gap:10px;padding:10px;border:1px solid var(--border);border-radius:12px;background:var(--surface-card)}img{width:64px;height:64px;border-radius:8px;object-fit:cover;background:var(--surface-muted)}article div{display:grid;gap:5px;min-width:0}span{color:var(--text-muted)}footer{display:flex;gap:12px;justify-content:center;align-items:center;margin-top:16px}footer button:disabled{opacity:.45}@media(max-width:700px){.page{padding:18px}.form{grid-template-columns:1fr}}`})export class DishesComponent{private readonly api=inject(AdminApiService);readonly categories=signal<Category[]>([]);readonly page=signal<DishPage>({items:[],page:1,pageSize:20,total:0});translations:CatalogTranslations=emptyTranslations();categoryId='';price=0;calories:number|undefined=undefined;imageUrl='';search='';constructor(){this.api.getCategories().subscribe({next:value=>{this.categories.set(value);this.categoryId=value[0]?.id??''}});this.load()}load(page=1){if(page<1)return;this.api.getDishes(page,20,this.search).subscribe({next:value=>this.page.set(value)})}save(){if(!this.categoryId||!this.price)return;this.api.createDish({categoryId:this.categoryId,translations:this.translations,priceAmount:this.price,calories:this.calories,imageUrl:this.imageUrl||undefined}).subscribe({next:()=>{this.translations=emptyTranslations();this.price=0;this.calories=undefined;this.imageUrl='';this.load()}})}upload(event:Event){const file=(event.target as HTMLInputElement).files?.[0];if(file)this.api.uploadImage(file).subscribe({next:value=>this.imageUrl=value.url})}dishName(dish:any){return dish.translations.find((translation:any)=>translation.languageCode==='ka')?.name??''}name=categoryName}
