import { Component, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { MatIconModule } from '@angular/material/icon';
import { AdminApiService, CatalogTranslations, Category, Dish, DishPage } from '../../core/api/admin-api.service';
import { CatalogImageComponent } from '../../shared/catalog-image/catalog-image.component';
import { DataTableComponent } from '../../shared/data-table/data-table.component';
import { BottomSheetComponent } from '../../shared/bottom-sheet/bottom-sheet.component';
import { emptyTranslations } from '../catalog/catalog-form.utils';
import { LanguageService } from '../../core/i18n/language.service';
import { ContextMenuAction, ContextMenuComponent } from '../../shared/context-menu/context-menu.component';

type DishDraft = { categoryId: string; translations: CatalogTranslations; imageUrl: string; priceAmount: number; calories: number | null; status: 'AVAILABLE' | 'PAUSED' | 'HIDDEN' };
const emptyDishDraft = (categoryId = ''): DishDraft => ({ categoryId, translations: emptyTranslations(), imageUrl: '', priceAmount: 0, calories: null, status: 'AVAILABLE' });

@Component({
  selector: 'app-dishes',
  imports: [FormsModule, MatIconModule, CatalogImageComponent, DataTableComponent, BottomSheetComponent, ContextMenuComponent],
  template: `
    <main class="page list-page">
      <section class="heading"><div><h1>{{ language.current() === 'ka' ? 'კერძები' : 'Dishes' }}</h1><p>{{ page().total }} {{ language.current() === 'ka' ? 'კერძი' : 'dishes' }}</p></div><button type="button" (click)="openCreate()"><mat-icon>add</mat-icon>{{ language.current() === 'ka' ? 'კერძის დამატება' : 'Add dish' }}</button></section>
      <app-data-table>
        <section data-table-filters class="filters"><label><mat-icon>search</mat-icon><input [(ngModel)]="search" (ngModelChange)="load(1)" [placeholder]="language.current() === 'ka' ? 'კერძის ძიება...' : 'Search dishes...'"></label><select [(ngModel)]="categoryFilter"><option value="">{{ language.current() === 'ka' ? 'ყველა კატ.' : 'All categories' }}</option>@for (category of categories(); track category.id) {<option [value]="category.id">{{ categoryName(category) }}</option>}</select><select [(ngModel)]="statusFilter"><option value="">{{ language.current() === 'ka' ? 'ყველა სტატ.' : 'All statuses' }}</option><option value="AVAILABLE">{{ language.current() === 'ka' ? 'აქტიური' : 'Active' }}</option><option value="PAUSED">{{ language.current() === 'ka' ? 'შეჩერებული' : 'Paused' }}</option><option value="HIDDEN">{{ language.current() === 'ka' ? 'დამალული' : 'Hidden' }}</option></select></section>
        <section data-table-rows class="table"><div class="head"><span>{{ language.current() === 'ka' ? 'კერძი' : 'Dish' }}</span><span>{{ language.current() === 'ka' ? 'კატეგორია' : 'Category' }}</span><span>{{ language.current() === 'ka' ? 'ფასი' : 'Price' }}</span><span>{{ language.current() === 'ka' ? 'კალ.' : 'Cal.' }}</span><span>{{ language.current() === 'ka' ? 'სტ.' : 'Status' }}</span><span>{{ language.current() === 'ka' ? 'მენ.' : 'Menus' }}</span><span></span></div>@for (dish of filtered(); track dish.id) {<article><div class="dish"><app-catalog-image [src]="dish.imageUrl"/><span><strong>{{ dishName(dish) }}</strong><small>{{ englishName(dish) }}</small></span></div><span>{{ categoryName(dish.category!) }}</span><b>{{ dish.priceAmount }} ₾</b><span>{{ dish.calories ?? '—' }}</span><span><i [class.paused]="dish.status === 'PAUSED'" [class.hidden]="dish.status === 'HIDDEN'">{{ statusName(dish.status) }}</i></span><span>1</span><button class="more" type="button" [attr.aria-label]="language.current() === 'ka' ? 'კერძის რედაქტირება' : 'Edit dish'" (click)="openActions(dish, $event)"><mat-icon>more_vert</mat-icon></button></article>}@empty {<div class="empty">{{ language.current() === 'ka' ? 'კერძები ვერ მოიძებნა' : 'No dishes found' }}</div>}</section>
        <footer data-table-footer><span>{{ page().total }} {{ language.current() === 'ka' ? 'კერძი' : 'dishes' }} · {{ page().page }}/{{ pages() }}</span><label class="page-jump">{{ language.current() === 'ka' ? 'გვერდი' : 'Page' }} <input type="number" [ngModel]="page().page" (ngModelChange)="goToPage($event)" min="1" [max]="pages()"></label><button type="button" (click)="load(page().page - 1)" [disabled]="page().page === 1"><mat-icon>chevron_left</mat-icon></button><button type="button" (click)="load(page().page + 1)" [disabled]="page().page >= pages()"><mat-icon>chevron_right</mat-icon></button></footer>
      </app-data-table>
    </main>
    <app-context-menu [open]="actionDish() !== null" [left]="actionPosition().x" [top]="actionPosition().y" [actions]="contextActions()" (selected)="runAction($event)" (closed)="closeActions()" />
    <app-bottom-sheet [open]="editorOpen()" [title]="editing() ? (language.current() === 'ka' ? 'კერძის რედაქტირება' : 'Edit dish') : (language.current() === 'ka' ? 'ახალი კერძი' : 'New dish')" eyebrow="KITCHEN & MENU" (closed)="closeEditor()">
      <form class="dish-form" (ngSubmit)="save()">
        <label>კატეგორია<select name="category" [(ngModel)]="form.categoryId">@for (category of categories(); track category.id) {<option [value]="category.id">{{ categoryName(category) }}</option>}</select></label>
        <div class="two-columns"><label>ფასი<input name="price" type="number" min="0" step="0.01" [(ngModel)]="form.priceAmount"></label><label>კალორიები<input name="calories" type="number" min="0" [(ngModel)]="form.calories"></label></div>
        <label>სტატუსი<select name="status" [(ngModel)]="form.status"><option value="AVAILABLE">აქტიური</option><option value="PAUSED">შეჩერებული</option><option value="HIDDEN">დამალული</option></select></label>
        <label>სურათის URL<input name="image" type="url" [(ngModel)]="form.imageUrl" placeholder="https://..."></label>
        <div class="translations"><label>ქართული სახელი<input name="ka" [(ngModel)]="form.translations.ka.name"></label><label>English name<input name="en" [(ngModel)]="form.translations.en.name"></label><label>Русское название<input name="ru" [(ngModel)]="form.translations.ru.name"></label></div>
        <div class="translations comments"><strong>{{ language.current() === 'ka' ? 'კერძის კომენტარები' : 'Dish comments' }}</strong><label>{{ language.current() === 'ka' ? 'ქართული კომენტარი' : 'Georgian comment' }}<textarea name="commentKa" [(ngModel)]="form.translations.ka.description"></textarea></label><label>{{ language.current() === 'ka' ? 'ინგლისური კომენტარი' : 'English comment' }}<textarea name="commentEn" [(ngModel)]="form.translations.en.description"></textarea></label><label>{{ language.current() === 'ka' ? 'რუსული კომენტარი' : 'Russian comment' }}<textarea name="commentRu" [(ngModel)]="form.translations.ru.description"></textarea></label></div>
        <footer><button type="button" class="cancel" (click)="closeEditor()">{{ language.current() === 'ka' ? 'გაუქმება' : 'Cancel' }}</button><button type="submit" [disabled]="saving()">{{ saving() ? (language.current() === 'ka' ? 'ინახება…' : 'Saving…') : (editing() ? (language.current() === 'ka' ? 'შენახვა' : 'Save changes') : (language.current() === 'ka' ? 'დამატება' : 'Add dish')) }}</button></footer>
      </form>
    </app-bottom-sheet>
  `,
  styles: `.page{min-height:calc(100dvh - 56px);padding:24px 30px;background:var(--color-background)}.heading{display:flex;justify-content:space-between;margin-bottom:24px}.heading h1{margin:0;font-size:29px}.heading p{margin:3px 0;color:var(--text-muted)}.heading button{display:flex;align-items:center;gap:7px;height:40px;padding:0 18px;border:0;border-radius:22px;background:var(--color-secondary);color:#fff;font-weight:800}.filters{display:flex;gap:15px}.filters label{display:flex;align-items:center;width:320px;height:46px;padding:0 14px;border:1px solid var(--border);border-radius:24px;background:var(--surface-card)}.filters input{width:100%;height:100%;margin-left:7px;border:0;background:transparent;outline:0}.filters select{height:46px;min-width:185px;padding:0 14px;border:1px solid var(--border);border-radius:24px;background:var(--surface-card)}.head,article{display:grid;grid-template-columns:2.2fr 1.25fr .9fr .65fr .9fr .55fr 34px;align-items:center;min-height:76px;padding:0 20px;gap:10px}.head{min-height:52px;background:var(--surface-muted);color:var(--text-muted);font-size:13px;font-weight:800}.head span:first-child{padding-left:58px}article{border-top:1px solid var(--border);color:var(--text-muted);font-size:14px}.dish{display:flex;align-items:center;gap:13px;color:var(--text-primary)}app-catalog-image{width:48px;height:48px;border-radius:50%}.dish strong,.dish small{display:block}.dish strong{font-size:16px}.dish small{margin-top:3px;color:var(--text-muted)}article b{color:var(--text-primary);font-size:15px}article i{padding:5px 11px;border-radius:13px;background:#285b28;color:#fff;font-style:normal;font-size:12px}.paused{border:1px solid #c76b18;background:#f6e2c9!important;color:#bb610d!important}.hidden{background:#34435e!important}.more,footer button{display:grid;place-items:center;border:0;background:transparent;color:var(--text-muted)}.more{width:30px;height:30px;border-radius:7px}.more:hover{background:var(--olive-soft);color:var(--olive)}footer{display:flex;align-items:center;gap:8px;min-height:42px;padding:0 20px;border-top:1px solid var(--border);color:var(--text-muted)}footer span:nth-child(2){flex:1}.page-jump{display:flex;align-items:center;gap:5px;font-size:12px}.page-jump input{width:42px;height:24px;padding:0 4px;border:1px solid var(--border);border-radius:6px;background:var(--surface-muted);text-align:center}.empty{padding:35px;text-align:center;color:var(--text-muted)}.dish-form{display:grid;gap:12px}.dish-form label{display:grid;gap:5px;color:var(--text-primary);font-size:13px;font-weight:700}.dish-form input,.dish-form select,.dish-form textarea{min-height:40px;padding:0 10px;border:1px solid var(--border);border-radius:8px;background:var(--surface-muted);font:inherit}.dish-form textarea{min-height:72px;padding-block:8px;resize:vertical}.two-columns{display:grid;grid-template-columns:1fr 1fr;gap:12px}.translations{display:grid;gap:12px;padding-top:4px;border-top:1px solid var(--border)}.comments strong{font-size:14px}.dish-form footer{display:flex;justify-content:flex-end;gap:8px}.dish-form footer button{padding:10px 14px;border:0;border-radius:8px;background:var(--olive);color:#fff;font-weight:800}.dish-form footer .cancel{background:var(--surface-muted);color:var(--text-primary)}@media(max-width:850px){.page{padding:18px}.filters select:last-child,.head,article>span:nth-of-type(2),article>span:nth-of-type(3),article>span:nth-of-type(4),article>span:nth-of-type(5){display:none}.head,article{grid-template-columns:1fr auto}.filters label{width:100%}}@media(max-width:500px){.two-columns{grid-template-columns:1fr}}`,
})
export class DishesComponent {
  private readonly api = inject(AdminApiService);
  readonly language = inject(LanguageService);
  readonly categories = signal<Category[]>([]);
  readonly page = signal<DishPage>({ items: [], page: 1, pageSize: 20, total: 0 });
  readonly editorOpen = signal(false);
  readonly editing = signal<Dish | null>(null);
  readonly saving = signal(false);
  readonly actionDish = signal<Dish | null>(null); readonly actionPosition = signal({ x: 0, y: 0 });
  search = ''; categoryFilter = ''; statusFilter = '';
  form: DishDraft = emptyDishDraft();

  constructor() { this.api.getCategories().subscribe((categories) => { this.categories.set(categories); this.form.categoryId = categories[0]?.id ?? ''; }); this.load(); }
  contextActions(): readonly ContextMenuAction[] { return [{ id: 'edit', label: this.language.current() === 'ka' ? 'რედაქტირება' : 'Edit dish', icon: 'edit' }]; }
  openActions(dish: Dish, event: MouseEvent): void { event.stopPropagation(); this.actionDish.set(dish); this.actionPosition.set({ x: Math.max(8, Math.min(event.clientX - 165, window.innerWidth - 190)), y: Math.max(8, Math.min(event.clientY - 6, window.innerHeight - 60)) }); }
  closeActions(): void { this.actionDish.set(null); }
  runAction(action: string): void { const dish = this.actionDish(); if (action === 'edit' && dish) this.openEdit(dish); }
  openCreate(): void { this.editing.set(null); this.form = emptyDishDraft(this.categories()[0]?.id ?? ''); this.editorOpen.set(true); }
  openEdit(dish: Dish): void { this.editing.set(dish); this.form = { categoryId: dish.categoryId, imageUrl: dish.imageUrl ?? '', priceAmount: Number(dish.priceAmount), calories: dish.calories, status: dish.status, translations: { ka: this.translation(dish, 'ka'), en: this.translation(dish, 'en'), ru: this.translation(dish, 'ru') } }; this.editorOpen.set(true); }
  closeEditor(): void { this.editorOpen.set(false); this.editing.set(null); }
  save(): void { if (!this.form.categoryId || this.form.priceAmount < 0 || !this.form.translations.ka.name.trim()) return; this.saving.set(true); const value = { ...this.form, imageUrl: this.form.imageUrl.trim() || undefined, calories: this.form.calories ?? undefined }; const current = this.editing(); const request = current ? this.api.updateDish(current.id, value) : this.api.createDish(value); request.subscribe({ next: () => { this.saving.set(false); this.closeEditor(); this.load(this.page().page); }, error: () => this.saving.set(false) }); }
  load(targetPage = 1): void { if (targetPage < 1) return; this.api.getDishes(targetPage, 20, this.search).subscribe((value) => this.page.set(value)); }
  filtered(): Dish[] { return this.page().items.filter((dish) => (!this.categoryFilter || dish.categoryId === this.categoryFilter) && (!this.statusFilter || dish.status === this.statusFilter)); }
  pages(): number { return Math.max(1, Math.ceil(this.page().total / this.page().pageSize)); }
  goToPage(value: number | string): void { this.load(Math.min(this.pages(), Math.max(1, Number(value) || 1))); }
  dishName(dish: Dish): string { return this.translation(dish, 'ka').name; }
  englishName(dish: Dish): string { return this.translation(dish, 'en').name; }
  categoryName(category: Category): string { return category?.translations.find((translation) => translation.languageCode === 'ka')?.name ?? '—'; }
  statusName(status: Dish['status']): string { return status === 'AVAILABLE' ? (this.language.current() === 'ka' ? 'აქტიური' : 'Active') : status === 'PAUSED' ? (this.language.current() === 'ka' ? 'შეჩერებული' : 'Paused') : (this.language.current() === 'ka' ? 'დამალული' : 'Hidden'); }
  private translation(dish: Dish, languageCode: string): { name: string; description?: string; recipe?: string } { const translation = dish.translations.find((item) => item.languageCode === languageCode); return { name: translation?.name ?? '', description: translation?.description ?? '', recipe: translation?.recipe }; }
}
